<h1>Contact</h1>

<div class='row'>

  <div class='col-md-6'>
    <p>Want to get in touch?</p>

    {{#link-to 'phone'}}Phone{{/link-to}}
    {{#link-to 'email'}}Email{{/link-to}}
    {{#unless messageSent}}
      <button class='btn btn-primary' {{action 'sendMessage'}}>Click here to hit up Dracula!</button>
    {{/unless}}
  </div>

  <div class='col-md-6'>
    {{outlet}}
  </div>
</div>
